---
# ============================================================================
# 01-apt-update.yml — Refresh apt and upgrade via the apt-base role.
# ----------------------------------------------------------------------------
# Thin wrapper. Real logic lives in roles/apt-base/.
# ============================================================================

- name: Refresh apt cache and apply upgrades
  hosts: k8s_cluster
  become: true
  gather_facts: false

  roles:
    - apt-base
