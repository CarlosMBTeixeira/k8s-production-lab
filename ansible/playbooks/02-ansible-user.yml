---
# ============================================================================
# 02-ansible-user.yml — Provision automation user via the ansible-user role.
# ----------------------------------------------------------------------------
# Thin wrapper. Real logic lives in roles/ansible-user/.
# ============================================================================

- name: Provision the dedicated automation user
  hosts: k8s_cluster
  become: true
  gather_facts: false

  roles:
    - ansible-user
